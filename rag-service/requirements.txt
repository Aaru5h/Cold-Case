# Cold Case Detective - RAG Dependencies (CPU-only, Render-optimized)

# PyTorch CPU-only (MUST be installed first via CPU index)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.2+cpu

# LangChain Core
langchain>=0.3.0
langchain-community>=0.3.0
langchain-groq>=0.2.0
langchain-huggingface>=0.1.0

# Embeddings (uses CPU torch above)
sentence-transformers>=2.2.0

# Vector Store
faiss-cpu>=1.7.4

# Environment Variables
python-dotenv>=1.0.0

# API Server
fastapi>=0.109.0
uvicorn>=0.27.0

# File Upload & PDF Support
python-multipart>=0.0.6
PyPDF2>=3.0.0
